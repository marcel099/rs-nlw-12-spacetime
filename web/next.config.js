/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      'i.ibb.co',
      '@API_IP'
    ],
  },
}

module.exports = nextConfig
